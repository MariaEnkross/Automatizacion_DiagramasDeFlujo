"""  if x_position == len(G.nodes()) - 2:  # Si es el penúltimo nodo
        pos[node] = (x_position - 4, -1)  # Colocar el penúltimo nodo en la posición (-4, -2)
        nx.draw_networkx_nodes(G, pos, nodelist=[node], node_size=3000, node_shape='d', node_color='skyblue')
    elif x_position == len(G.nodes()) - 1:  # Si es el último nodo
        pos[node] = (x_position - 4, -3.5)  # Colocar el último nodo en la posición (-3, -2)
        nx.draw_networkx_nodes(G, pos, nodelist=[node], node_size=3000, node_shape='s', node_color='lightgreen')
    elif x_position == 1 or x_position == 3:  # Si es el primer nodo o el tercer nodo, dibujamos un rombo
            pos[node] = (x_position, y_position)  # Posición para el primer nodo
            nx.draw_networkx_nodes(G, pos, nodelist=[node], node_size=3000, node_shape='d', node_color='skyblue')
    else: # Si es cualquier otro nodo, dibujamos un rectángulo
            pos[node] = (x_position, y_position)
            nx.draw_networkx_nodes(G, pos, nodelist=[node], node_size=3000, node_shape='s', node_color='lightgreen')
    x_position += 1 """